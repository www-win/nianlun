# 预置 AI 接入信息（复制本文件为 .env 后填写真实值）
#
# ⚠️ 安全提示：本项目是纯前端应用，没有后端。
# 这里填写的 API Key 会被打包进 JS 并下发到浏览器，
# 任何访问站点的人都能在源码 / 网络请求中看到它，等同于公开。
# 仅在你接受这一点（例如内部使用、临时演示、低权限只读 key）时使用。

VITE_AI_BASE_URL=https://api.gaccode.com
VITE_AI_API_KEY=
VITE_AI_MODEL=claude-opus-4-8
