# 年轮小程序 · 云备份/恢复 + 样本放大 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把小程序的全部处理后数据自动备份到微信云开发，本地被清空后打开即自动恢复，无需重新导入；并把每人聊天样本从 30 放大到 60。

**Architecture:** 沿用现有 `core（纯逻辑）→ miniapp adapters（wx 只在函数体内）→ stores（Pinia）→ pages` 分层。新增 `cloudBackup` 适配器负责「采集快照→gzip→上传」与「下载→解压→写回」，全程依赖注入以便 Vitest 内存测试；真机 wx.cloud 接线与云函数是薄封装、真机验证。快照单位是「整个 `nianlun:` KV 命名空间 + `nianlun_store` 文件夹」，面向未来自动纳入新数据。

**Tech Stack:** TypeScript、uni-app（mp-weixin）、Pinia、Vitest、fflate（gzip，已是依赖）、微信云开发（云存储 + 云数据库 + 云函数）。

## Global Constraints

- **core 不碰 DOM/wx/vue**：本计划不改 `@nianlun/core` 的运行时逻辑（仅调用其既有 `extractFriendSamples`）。
- **wx 只在函数体内引用**：所有 `wx.*` 访问必须惰性（在方法体内），模块顶层不触碰，否则 node 单测收集阶段抛 `ReferenceError`（既有约定，见 rawStore.ts 注释）。
- **备份内容**：整个 `nianlun:` 前缀 KV（排除 legacy）+ 文件数据集 `['friends','samples','recentInsights','recentSamples','stocks']`。
- **样本参数**：miniapp 侧 `maxPerFriend: 60, maxChars: 120`；core 默认值（30/80）不动。
- **gzip**：`fflate` `gzipSync(data, { level: 4 })`。
- **免双重序列化**：文件段用磁盘原始 JSON 字符串直传（`readRaw`/`writeRaw`），不 parse→stringify。
- **体积阈值**：`BIG_THRESHOLD = 8 * 1024 * 1024`（8MB），超过切按数据集分块上传。
- **恢复触发**：仅当本地 `friends.length === 0` 时自动恢复；覆盖式导入前手动按钮弹确认。
- **云环境**：`cloud1-d4gzww8dp909b47cb`（已在 App.vue 初始化）。
- **测试命令**：`pnpm --filter @nianlun/miniapp exec vitest run <file>`。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `packages/miniapp/src/adapters/fsStore.ts` | 改 | `FsJsonBackend` 增 `readRaw/writeRaw`（原始字符串直存取） |
| `packages/miniapp/src/adapters/storage.ts` | 改 | 增 `exportAll/importAll`（整命名空间快照） |
| `packages/miniapp/src/adapters/cloudBackup.ts` | 新增 | `backup()/restore()`：采集→gzip→上传、下载→解压→写回；含分块降级 |
| `packages/miniapp/src/stores/backup.ts` | 新增 | 防抖调度、状态、`backupNow/restoreNow`、重入保护 |
| `packages/miniapp/src/stores/data.ts` | 改 | `setData/updateFriend` 保存后触发 `scheduleBackup` |
| `packages/miniapp/src/adapters/parseLocal.ts` | 改 | `extractFriendSamples` 传 `{maxPerFriend:60,maxChars:120}`（两处样本） |
| `packages/miniapp/src/App.vue` | 改 | `hydrate` 后本地空则自动 `restore` 再 `hydrate` |
| `packages/miniapp/src/pages/overview/overview.vue` | 改 | 「立即备份 / 从云恢复」按钮 + 上次备份时间 |
| `packages/miniapp/cloudfunctions/getOpenId/` | 新增 | 返回 openid（真机接线用） |

---

## Task 1: 文件后端原始字符串直存取（readRaw/writeRaw）

**Files:**
- Modify: `packages/miniapp/src/adapters/fsStore.ts`
- Test: `packages/miniapp/src/adapters/__tests__/fsStore.test.ts`

**Interfaces:**
- Produces:
  - `FsJsonBackend.readRaw(name: string): string | undefined`
  - `FsJsonBackend.writeRaw(name: string, raw: string): void`
  - `makeFsJson`/`makeKvFsJson` 均实现上述两方法。

- [ ] **Step 1: 写失败测试**

追加到 `packages/miniapp/src/adapters/__tests__/fsStore.test.ts`（若无则创建；顶部已有的 import 保留）：

```ts
import { describe, it, expect } from 'vitest'
import { makeKvFsJson } from '../fsStore'

function memKv() {
  const m = new Map<string, unknown>()
  return { get: (k: string) => (m.has(k) ? m.get(k) : ''), set: (k: string, v: unknown) => void m.set(k, v), remove: (k: string) => void m.delete(k), _m: m }
}

describe('FsJsonBackend readRaw/writeRaw (kv)', () => {
  it('writeRaw 存原始字符串，readRaw 原样取回', () => {
    const fs = makeKvFsJson(memKv())
    fs.writeRaw('friends', '[{"id":"a"}]')
    expect(fs.readRaw('friends')).toBe('[{"id":"a"}]')
  })
  it('readRaw 缺失返回 undefined', () => {
    const fs = makeKvFsJson(memKv())
    expect(fs.readRaw('nope')).toBeUndefined()
  })
  it('writeRaw 写入后 read（解析版）也能读到对象', () => {
    const fs = makeKvFsJson(memKv())
    fs.writeRaw('friends', '[{"id":"a"}]')
    expect(fs.read('friends')).toEqual([{ id: 'a' }])
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/fsStore.test.ts`
Expected: FAIL（`readRaw`/`writeRaw` 不是函数）

- [ ] **Step 3: 实现**

在 `fsStore.ts` 的 `FsJsonBackend` 接口加两方法，并在两个工厂里实现：

```ts
export interface FsJsonBackend {
  read(name: string): unknown
  write(name: string, data: unknown): void
  remove(name: string): void
  /** 读磁盘上的原始 JSON 文本（不解析）；不存在返回 undefined。 */
  readRaw(name: string): string | undefined
  /** 直接把原始 JSON 文本落盘（不再序列化）。 */
  writeRaw(name: string, raw: string): void
}
```

`makeFsJson` 返回对象内新增：

```ts
    readRaw(name) {
      try { return fs.readFile(path(name)) } catch { return undefined }
    },
    writeRaw(name, raw) {
      fs.ensureDir(baseDir)
      fs.writeFile(path(name), raw)
    },
```

`makeKvFsJson` 返回对象内新增：

```ts
    readRaw(name) {
      const v = kv.get(key(name))
      return v === '' || v === undefined || v === null ? undefined : JSON.stringify(v)
    },
    writeRaw(name, raw) { kv.set(key(name), JSON.parse(raw)) },
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/fsStore.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/adapters/fsStore.ts packages/miniapp/src/adapters/__tests__/fsStore.test.ts
git commit -m "feat(miniapp): fsStore 增 readRaw/writeRaw 原始字符串直存取"
```

---

## Task 2: storage 整命名空间快照（exportAll/importAll）

**Files:**
- Modify: `packages/miniapp/src/adapters/storage.ts`
- Test: `packages/miniapp/src/adapters/__tests__/storage.test.ts`

**Interfaces:**
- Consumes: `FsJsonBackend.readRaw/writeRaw`（Task 1）、`StorageBackend.keys?()`。
- Produces:
  - `interface StorageSnapshot { kv: Record<string, unknown>; files: Record<string, string> }`
  - `makeStorage(...).exportAll(): StorageSnapshot`
  - `makeStorage(...).importAll(snap: StorageSnapshot): void`
  - 导出常量 `BACKUP_FILE_DATASETS: readonly string[]`

- [ ] **Step 1: 写失败测试**

追加到 `packages/miniapp/src/adapters/__tests__/storage.test.ts`：

```ts
import { makeStorage } from '../storage'
import { makeKvFsJson } from '../fsStore'

function memBackend() {
  const m = new Map<string, unknown>()
  return {
    get: (k: string) => (m.has(k) ? m.get(k) : ''),
    set: (k: string, v: unknown) => void m.set(k, v),
    remove: (k: string) => void m.delete(k),
    keys: () => [...m.keys()],
    _m: m,
  }
}

describe('storage exportAll/importAll', () => {
  it('往返：export 后 import 到新 storage，数据等价', () => {
    const b1 = memBackend()
    const s1 = makeStorage(b1, makeKvFsJson(b1))
    s1.saveReport({ year: 2025, totalMessages: 3, friendCount: 1, activeDays: 2, topContacts: [], latestMessage: null, keywords: [], relationBreakdown: [] } as any)
    s1.saveFriends([{ id: 'a', name: '甲', msgCount: 5 } as any])
    s1.saveStockPicks([{ code: '600000' } as any])

    const snap = s1.exportAll()

    const b2 = memBackend()
    const s2 = makeStorage(b2, makeKvFsJson(b2))
    s2.importAll(snap)
    expect(s2.loadReport()?.year).toBe(2025)
    expect(s2.loadFriends()[0].id).toBe('a')
    expect(s2.loadStockPicks()[0].code).toBe('600000')
  })

  it('exportAll 忽略 legacy 键与非 nianlun 键', () => {
    const b = memBackend()
    const s = makeStorage(b, makeKvFsJson(b))
    s.saveReport({ year: 2025 } as any)
    b.set('nianlun:raw:0', 'x')          // legacy
    b.set('nianlun:rawIndex', { count: 1 }) // legacy
    b.set('other:thing', 'y')             // 非 nianlun
    const snap = s.exportAll()
    expect(Object.keys(snap.kv)).toContain('nianlun:report')
    expect(Object.keys(snap.kv)).not.toContain('nianlun:raw:0')
    expect(Object.keys(snap.kv)).not.toContain('nianlun:rawIndex')
    expect(Object.keys(snap.kv)).not.toContain('other:thing')
  })

  it('importAll 只增不删（不清除未覆盖的已有键）', () => {
    const b = memBackend()
    const s = makeStorage(b, makeKvFsJson(b))
    s.saveMyBazi({ y: 1 } as any)
    s.importAll({ kv: { 'nianlun:report': { year: 2025 } }, files: {} })
    expect(s.loadReport()?.year).toBe(2025)
    expect(s.loadMyBazi()).toEqual({ y: 1 }) // 未被清掉
  })
})
```

> 注：`makeKvFsJson` 用同一 `backend` 时，文件段实际以 `nianlun:fsjson:*` 键存于 KV。exportAll 的 KV 采集**必须排除** `nianlun:fsjson:` 前缀，避免文件数据被重复计入 kv 段。

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/storage.test.ts`
Expected: FAIL（`exportAll` 不是函数）

- [ ] **Step 3: 实现**

在 `storage.ts` 顶部常量区加：

```ts
/** 进备份的「大数据文件」数据集清单；新增文件数据集须在此登记。 */
export const BACKUP_FILE_DATASETS = ['friends', 'samples', 'recentInsights', 'recentSamples', 'stocks'] as const
export interface StorageSnapshot { kv: Record<string, unknown>; files: Record<string, string> }

const LEGACY_KV_PREFIXES = ['nianlun:raw:', 'nianlun:fsjson:']
const LEGACY_KV_EXACT = new Set<string>(['nianlun:rawIndex', ...['nianlun:friends', 'nianlun:samples', 'nianlun:recentInsights', 'nianlun:recentSamples', 'nianlun:stocks']])
function isBackupKvKey(k: string): boolean {
  if (!k.startsWith('nianlun:')) return false
  if (LEGACY_KV_EXACT.has(k)) return false
  return !LEGACY_KV_PREFIXES.some((p) => k.startsWith(p))
}
```

在 `makeStorage` 的 `return { ... }` 里加两个方法（与既有方法并列）：

```ts
    exportAll(): StorageSnapshot {
      const kv: Record<string, unknown> = {}
      const keys = backend.keys?.() ?? []
      for (const k of keys) if (isBackupKvKey(k)) kv[k] = backend.get(k)
      const files: Record<string, string> = {}
      for (const name of BACKUP_FILE_DATASETS) {
        const raw = fs.readRaw(name)
        if (raw !== undefined) files[name] = raw
      }
      return { kv, files }
    },
    importAll(snap: StorageSnapshot): void {
      for (const [k, v] of Object.entries(snap.kv ?? {})) backend.set(k, v)
      for (const [name, raw] of Object.entries(snap.files ?? {})) fs.writeRaw(name, raw)
    },
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/storage.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/adapters/storage.ts packages/miniapp/src/adapters/__tests__/storage.test.ts
git commit -m "feat(miniapp): storage 增 exportAll/importAll 整命名空间快照"
```

---

## Task 3: cloudBackup 适配器（单包备份/恢复）

**Files:**
- Create: `packages/miniapp/src/adapters/cloudBackup.ts`
- Test: `packages/miniapp/src/adapters/__tests__/cloudBackup.test.ts`

**Interfaces:**
- Consumes: `StorageSnapshot`、`exportAll/importAll`（Task 2）。
- Produces:
  - `interface BackupEnvelope { version: 1; createdAt: number; kv: Record<string,unknown>; files: Record<string,string> }`
  - `interface CloudBackupDeps { storage; upload; download; gzip; gunzip; now }`（见下）
  - `makeCloudBackup(deps): { backup(): Promise<{ bytes: number }>; restore(): Promise<boolean> }`

- [ ] **Step 1: 写失败测试**

创建 `packages/miniapp/src/adapters/__tests__/cloudBackup.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { gzipSync, gunzipSync } from 'fflate'
import { makeCloudBackup } from '../cloudBackup'
import type { StorageSnapshot } from '../storage'

function memStorage(initial: StorageSnapshot) {
  let snap = initial
  return {
    exportAll: () => snap,
    importAll: (s: StorageSnapshot) => { snap = s },
    _get: () => snap,
  }
}
/** 内存云：按 cloudPath 存字节。 */
function memCloud() {
  const files = new Map<string, Uint8Array>()
  return {
    upload: async (p: string, bytes: Uint8Array) => void files.set(p, bytes),
    download: async (p: string) => (files.has(p) ? files.get(p)! : null),
    _files: files,
  }
}

const deps = (storage: any, cloud: any) => ({
  storage, upload: cloud.upload, download: cloud.download,
  gzip: (d: Uint8Array) => gzipSync(d, { level: 4 }), gunzip: gunzipSync,
  now: () => 1_700_000_000_000,
})

describe('cloudBackup 单包', () => {
  it('backup 后 restore 到另一个 storage，数据等价', async () => {
    const src = memStorage({ kv: { 'nianlun:report': { year: 2025 } }, files: { friends: '[{"id":"a"}]' } })
    const cloud = memCloud()
    const cbSrc = makeCloudBackup(deps(src, cloud))
    const res = await cbSrc.backup()
    expect(res.bytes).toBeGreaterThan(0)

    const dst = memStorage({ kv: {}, files: {} })
    const cbDst = makeCloudBackup(deps(dst, cloud))
    const ok = await cbDst.restore()
    expect(ok).toBe(true)
    expect(dst._get()).toEqual({ kv: { 'nianlun:report': { year: 2025 } }, files: { friends: '[{"id":"a"}]' } })
  })

  it('云端无备份时 restore 返回 false', async () => {
    const dst = memStorage({ kv: {}, files: {} })
    const cb = makeCloudBackup(deps(dst, memCloud()))
    expect(await cb.restore()).toBe(false)
  })

  it('上传的确实是 gzip 压缩后的字节（能被 gunzip 还原成含 version 的信封）', async () => {
    const src = memStorage({ kv: {}, files: { friends: '[]' } })
    const cloud = memCloud()
    await makeCloudBackup(deps(src, cloud)).backup()
    const bytes = [...cloud._files.values()][0]
    const json = new TextDecoder().decode(gunzipSync(bytes))
    const env = JSON.parse(json)
    expect(env.version).toBe(1)
    expect(env.files.friends).toBe('[]')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/cloudBackup.test.ts`
Expected: FAIL（找不到模块 `../cloudBackup`）

- [ ] **Step 3: 实现**

创建 `packages/miniapp/src/adapters/cloudBackup.ts`：

```ts
import type { StorageSnapshot } from './storage'

export interface BackupEnvelope {
  version: 1
  createdAt: number
  kv: Record<string, unknown>
  files: Record<string, string>
}

export interface CloudBackupDeps {
  storage: { exportAll(): StorageSnapshot; importAll(s: StorageSnapshot): void }
  /** 上传字节到逻辑路径（wx 实现映射到按用户隔离的云存储路径）。 */
  upload(cloudPath: string, bytes: Uint8Array): Promise<void>
  /** 下载逻辑路径的字节；不存在返回 null。 */
  download(cloudPath: string): Promise<Uint8Array | null>
  gzip(data: Uint8Array): Uint8Array
  gunzip(data: Uint8Array): Uint8Array
  now(): number
}

const SINGLE_PATH = 'backup.json.gz'
const enc = new TextEncoder()
const dec = new TextDecoder()

export function makeCloudBackup(deps: CloudBackupDeps) {
  async function backup(): Promise<{ bytes: number }> {
    const snap = deps.storage.exportAll()
    const env: BackupEnvelope = { version: 1, createdAt: deps.now(), kv: snap.kv, files: snap.files }
    const bytes = deps.gzip(enc.encode(JSON.stringify(env)))
    await deps.upload(SINGLE_PATH, bytes)
    return { bytes: bytes.length }
  }

  async function restore(): Promise<boolean> {
    const bytes = await deps.download(SINGLE_PATH)
    if (!bytes) return false
    const env = JSON.parse(dec.decode(deps.gunzip(bytes))) as BackupEnvelope
    if (env.version !== 1) throw new Error(`不支持的备份版本：${env.version}`)
    deps.storage.importAll({ kv: env.kv, files: env.files })
    return true
  }

  return { backup, restore }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/cloudBackup.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/adapters/cloudBackup.ts packages/miniapp/src/adapters/__tests__/cloudBackup.test.ts
git commit -m "feat(miniapp): cloudBackup 单包备份/恢复(采集→gzip→上传/下载→解压→写回)"
```

---

## Task 4: cloudBackup 分块降级（超阈值按数据集上传）

**Files:**
- Modify: `packages/miniapp/src/adapters/cloudBackup.ts`
- Test: `packages/miniapp/src/adapters/__tests__/cloudBackup.test.ts`

**Interfaces:**
- Consumes: Task 3 的 deps/信封。
- Produces: `makeCloudBackup(deps, opts?: { bigThreshold?: number })`；分块时写 `manifest.json.gz` + `parts/{name}.json.gz`；`restore` 自动识别单包/分块。

- [ ] **Step 1: 写失败测试**

追加到 `cloudBackup.test.ts`：

```ts
describe('cloudBackup 分块降级', () => {
  it('超阈值时分块上传，restore 能还原', async () => {
    const big = 'x'.repeat(50) // 配合极小阈值触发分块
    const src = memStorage({ kv: { 'nianlun:report': { note: big } }, files: { friends: `["${big}"]`, stocks: '[1,2,3]' } })
    const cloud = memCloud()
    const cb = makeCloudBackup(deps(src, cloud), { bigThreshold: 10 })
    await cb.backup()
    // 应写入 manifest + 多个 part，而非单一 backup.json.gz
    const paths = [...cloud._files.keys()]
    expect(paths).toContain('manifest.json.gz')
    expect(paths.some((p) => p.startsWith('parts/'))).toBe(true)
    expect(paths).not.toContain('backup.json.gz')

    const dst = memStorage({ kv: {}, files: {} })
    const ok = await makeCloudBackup(deps(dst, cloud), { bigThreshold: 10 }).restore()
    expect(ok).toBe(true)
    expect(dst._get()).toEqual(src._get())
  })

  it('阈值内仍走单包', async () => {
    const src = memStorage({ kv: {}, files: { friends: '[]' } })
    const cloud = memCloud()
    await makeCloudBackup(deps(src, cloud), { bigThreshold: 8 * 1024 * 1024 }).backup()
    expect([...cloud._files.keys()]).toEqual(['backup.json.gz'])
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/cloudBackup.test.ts`
Expected: FAIL（分块用例失败：仍写单包 / manifest 缺失）

- [ ] **Step 3: 实现**

改写 `cloudBackup.ts`，把 `makeCloudBackup` 增 `opts` 与分块逻辑：

```ts
const SINGLE_PATH = 'backup.json.gz'
const MANIFEST_PATH = 'manifest.json.gz'
const DEFAULT_BIG_THRESHOLD = 8 * 1024 * 1024

interface Manifest { version: 1; createdAt: number; chunked: true; kvKeys: string[]; fileNames: string[] }

export function makeCloudBackup(deps: CloudBackupDeps, opts: { bigThreshold?: number } = {}) {
  const bigThreshold = opts.bigThreshold ?? DEFAULT_BIG_THRESHOLD
  const gz = (obj: unknown) => deps.gzip(enc.encode(JSON.stringify(obj)))
  const ungz = (bytes: Uint8Array) => JSON.parse(dec.decode(deps.gunzip(bytes)))

  function estimate(snap: StorageSnapshot): number {
    let n = JSON.stringify(snap.kv).length
    for (const v of Object.values(snap.files)) n += v.length
    return n
  }

  async function backup(): Promise<{ bytes: number }> {
    const snap = deps.storage.exportAll()
    if (estimate(snap) <= bigThreshold) {
      const bytes = gz({ version: 1, createdAt: deps.now(), kv: snap.kv, files: snap.files } as BackupEnvelope)
      await deps.upload(SINGLE_PATH, bytes)
      return { bytes: bytes.length }
    }
    // 分块：每个文件数据集一个 part，KV 整体一个 part
    let total = 0
    const kvBytes = gz(snap.kv)
    await deps.upload('parts/kv.json.gz', kvBytes); total += kvBytes.length
    const fileNames = Object.keys(snap.files)
    for (const name of fileNames) {
      const b = gz(snap.files[name])
      await deps.upload(`parts/file-${name}.json.gz`, b); total += b.length
    }
    const manifest: Manifest = { version: 1, createdAt: deps.now(), chunked: true, kvKeys: Object.keys(snap.kv), fileNames }
    const mBytes = gz(manifest)
    await deps.upload(MANIFEST_PATH, mBytes); total += mBytes.length
    return { bytes: total }
  }

  async function restore(): Promise<boolean> {
    const single = await deps.download(SINGLE_PATH)
    if (single) {
      const env = ungz(single) as BackupEnvelope
      if (env.version !== 1) throw new Error(`不支持的备份版本：${env.version}`)
      deps.storage.importAll({ kv: env.kv, files: env.files })
      return true
    }
    const mBytes = await deps.download(MANIFEST_PATH)
    if (!mBytes) return false
    const manifest = ungz(mBytes) as Manifest
    if (manifest.version !== 1) throw new Error(`不支持的备份版本：${manifest.version}`)
    const kvPart = await deps.download('parts/kv.json.gz')
    const kv = kvPart ? (ungz(kvPart) as Record<string, unknown>) : {}
    const files: Record<string, string> = {}
    for (const name of manifest.fileNames) {
      const b = await deps.download(`parts/file-${name}.json.gz`)
      if (b) files[name] = ungz(b) as string
    }
    deps.storage.importAll({ kv, files })
    return true
  }

  return { backup, restore }
}
```

> 保留 `SINGLE_PATH` 优先：restore 先看单包，没有再看 manifest，兼容两种历史备份。备份时二选一（不会同时存在两份新数据，但旧单包可能残留——真机接线任务里在分块成功后删除旧 `backup.json.gz`，见 Task 9）。

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/cloudBackup.test.ts`
Expected: PASS（全部用例）

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/adapters/cloudBackup.ts packages/miniapp/src/adapters/__tests__/cloudBackup.test.ts
git commit -m "feat(miniapp): cloudBackup 超阈值分块上传/恢复(降内存峰值)"
```

---

## Task 5: backup store（防抖 + 状态 + 手动动作）

**Files:**
- Create: `packages/miniapp/src/stores/backup.ts`
- Test: `packages/miniapp/src/stores/__tests__/backup.test.ts`

**Interfaces:**
- Consumes: `makeCloudBackup` 的返回类型 `{ backup(): Promise<{bytes:number}>; restore(): Promise<boolean> }`。
- Produces: `createBackupStore({ cloudBackup, storage, delayMs?, schedule? })` → Pinia store，暴露 `status`、`lastBackupAt`、`error`、`scheduleBackup()`、`backupNow()`、`restoreNow()`、`flush()`（测试用，立即执行挂起的防抖）。

- [ ] **Step 1: 写失败测试**

创建 `packages/miniapp/src/stores/__tests__/backup.test.ts`：

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { createBackupStore } from '../backup'

function fakeCloud() {
  return { backup: vi.fn(async () => ({ bytes: 10 })), restore: vi.fn(async () => true) }
}
function fakeStorage() {
  let ts: number | null = null
  return { saveLastBackupAt: (t: number) => { ts = t }, loadLastBackupAt: () => ts }
}
/** 手动可控的防抖：记录回调，flush 时执行。 */
function manualSchedule() {
  let pending: (() => void) | null = null
  return { schedule: (fn: () => void) => { pending = fn }, run: () => { const p = pending; pending = null; p?.() } }
}

describe('backup store', () => {
  beforeEach(() => setActivePinia(createPinia()))

  it('scheduleBackup 多次只在 flush 后备份一次', async () => {
    const cloud = fakeCloud(); const sched = manualSchedule()
    const useStore = createBackupStore({ cloudBackup: cloud, storage: fakeStorage(), schedule: sched.schedule })
    const s = useStore()
    s.scheduleBackup(); s.scheduleBackup(); s.scheduleBackup()
    expect(cloud.backup).not.toHaveBeenCalled()
    sched.run(); await Promise.resolve(); await Promise.resolve()
    expect(cloud.backup).toHaveBeenCalledTimes(1)
  })

  it('backupNow 成功后更新 lastBackupAt', async () => {
    const cloud = fakeCloud(); const storage = fakeStorage()
    const useStore = createBackupStore({ cloudBackup: cloud, storage, schedule: (fn) => fn() })
    const s = useStore()
    await s.backupNow()
    expect(storage.loadLastBackupAt()).not.toBeNull()
    expect(s.status).toBe('idle')
  })

  it('备份进行中，再次 backupNow 被重入保护忽略', async () => {
    let release: () => void = () => {}
    const cloud = { backup: vi.fn(() => new Promise<{ bytes: number }>((r) => { release = () => r({ bytes: 1 }) })), restore: vi.fn() }
    const useStore = createBackupStore({ cloudBackup: cloud as any, storage: fakeStorage(), schedule: (fn) => fn() })
    const s = useStore()
    const p1 = s.backupNow()
    const p2 = s.backupNow() // 应被忽略
    release(); await p1; await p2
    expect(cloud.backup).toHaveBeenCalledTimes(1)
  })

  it('restoreNow 云端无备份返回 false，不报错', async () => {
    const cloud = { backup: vi.fn(), restore: vi.fn(async () => false) }
    const useStore = createBackupStore({ cloudBackup: cloud as any, storage: fakeStorage(), schedule: (fn) => fn() })
    const s = useStore()
    expect(await s.restoreNow()).toBe(false)
    expect(s.status).toBe('idle')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/stores/__tests__/backup.test.ts`
Expected: FAIL（找不到 `../backup`）

- [ ] **Step 3: 实现**

创建 `packages/miniapp/src/stores/backup.ts`：

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

type CloudBackup = { backup(): Promise<{ bytes: number }>; restore(): Promise<boolean> }
type LastAtStore = { saveLastBackupAt(t: number): void; loadLastBackupAt(): number | null }
type ScheduleFn = (fn: () => void) => void

export interface BackupDeps {
  cloudBackup: CloudBackup
  storage: LastAtStore
  /** 防抖调度器；默认 setTimeout(delayMs)。测试注入可控实现。 */
  schedule?: ScheduleFn
  delayMs?: number
}

export function createBackupStore(deps: BackupDeps) {
  const delayMs = deps.delayMs ?? 5000
  let timer: ReturnType<typeof setTimeout> | null = null
  const schedule: ScheduleFn = deps.schedule ?? ((fn) => {
    if (timer) clearTimeout(timer)
    timer = setTimeout(fn, delayMs)
  })

  return defineStore('backup', () => {
    const status = ref<'idle' | 'backing' | 'restoring' | 'error'>('idle')
    const lastBackupAt = ref<number | null>(deps.storage.loadLastBackupAt())
    const error = ref('')

    async function doBackup(): Promise<void> {
      if (status.value === 'backing' || status.value === 'restoring') return // 重入保护
      status.value = 'backing'; error.value = ''
      try {
        await deps.cloudBackup.backup()
        const now = Date.now()
        deps.storage.saveLastBackupAt(now); lastBackupAt.value = now
        status.value = 'idle'
      } catch (e) {
        error.value = (e as Error).message; status.value = 'error'
      }
    }

    function scheduleBackup(): void { schedule(() => { void doBackup() }) }
    async function backupNow(): Promise<void> { await doBackup() }

    async function restoreNow(): Promise<boolean> {
      if (status.value === 'backing' || status.value === 'restoring') return false
      status.value = 'restoring'; error.value = ''
      try {
        const ok = await deps.cloudBackup.restore()
        status.value = 'idle'
        return ok
      } catch (e) {
        error.value = (e as Error).message; status.value = 'error'
        return false
      }
    }

    return { status, lastBackupAt, error, scheduleBackup, backupNow, restoreNow }
  })
}
```

> `saveLastBackupAt/loadLastBackupAt` 需在 storage 上新增（KV 键 `nianlun:lastBackupAt`）。在 Task 5 一并加到 `storage.ts`：
> ```ts
> saveLastBackupAt(t: number): void { backend.set('nianlun:lastBackupAt', t) },
> loadLastBackupAt(): number | null { const v = backend.get('nianlun:lastBackupAt'); return typeof v === 'number' ? v : null },
> ```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/stores/__tests__/backup.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/stores/backup.ts packages/miniapp/src/stores/__tests__/backup.test.ts packages/miniapp/src/adapters/storage.ts
git commit -m "feat(miniapp): backup store 防抖调度+状态+手动备份/恢复+重入保护"
```

---

## Task 6: data store 保存后触发防抖备份

**Files:**
- Modify: `packages/miniapp/src/stores/data.ts`
- Test: `packages/miniapp/src/stores/__tests__/data.test.ts`

**Interfaces:**
- Consumes: 一个 `onSaved?: () => void` 回调（由 data store 在每次持久化后调用）。
- Produces: `createDataStore(storage, rawStore, onSaved?)` 第三参可注入；`setData`/`updateFriend` 保存后调用 `onSaved`。

> 设计说明：为避免 data store 直接依赖 backup store（循环依赖），用注入回调 `onSaved`。运行时在 `main.ts`/`App.vue` 装配：`createDataStore(storage, rawStore, () => useBackupStore().scheduleBackup())`。但 `useDataStore` 已是模块级单例导出，故改为「setter 注入」：新增 `setOnSaved(fn)`。

- [ ] **Step 1: 写失败测试**

追加到 `packages/miniapp/src/stores/__tests__/data.test.ts`：

```ts
it('setData 后触发 onSaved 回调', async () => {
  const spy = vi.fn()
  const useData = createDataStore(memStorage(), memRawStore())
  const d = useData()
  d.setOnSaved(spy)
  await d.setData([{ id: 'a' } as any], { year: 2025 } as any)
  expect(spy).toHaveBeenCalledTimes(1)
})

it('updateFriend 后触发 onSaved 回调', async () => {
  const spy = vi.fn()
  const useData = createDataStore(memStorage(), memRawStore())
  const d = useData()
  await d.setData([{ id: 'a', userEdited: {} } as any], { year: 2025 } as any)
  d.setOnSaved(spy)
  await d.updateFriend('a', { alias: '小甲' })
  expect(spy).toHaveBeenCalledTimes(1)
})
```

> 若测试文件还没有 `memStorage/memRawStore` 辅助，复用其现有的注入辅助；`vi` 从 `vitest` import。

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/stores/__tests__/data.test.ts`
Expected: FAIL（`setOnSaved` 不是函数）

- [ ] **Step 3: 实现**

改 `packages/miniapp/src/stores/data.ts`：`createDataStore` 内维护 `onSaved` 并在保存点调用。

```ts
  return defineStore('data', () => {
    const friends = ref<Friend[]>([])
    const report = ref<ReportData | null>(null)
    const hasData = computed(() => friends.value.length > 0)
    let onSaved: (() => void) | null = null
    function setOnSaved(fn: () => void) { onSaved = fn }
    const fireSaved = () => { try { onSaved?.() } catch { /* 备份触发失败不影响本地保存 */ } }

    async function hydrate() {
      friends.value = storage.loadFriends()
      report.value = storage.loadReport()
    }
    async function setData(newFriends: Friend[], newReport: ReportData) {
      friends.value = newFriends
      report.value = newReport
      storage.saveFriends(JSON.parse(JSON.stringify(newFriends)))
      storage.saveReport(JSON.parse(JSON.stringify(newReport)))
      fireSaved()
    }
    async function updateFriend(id: string, patch: { role?: string; rel?: Relation; alias?: string; mbti?: MbtiCode | null }) {
      const f = friends.value.find((x) => x.id === id)
      if (!f) return
      if (patch.role !== undefined) { f.role = patch.role; f.userEdited.role = patch.role }
      if (patch.rel !== undefined) { f.rel = patch.rel; f.userEdited.rel = patch.rel }
      if (patch.alias !== undefined) { f.alias = patch.alias; f.userEdited.alias = patch.alias }
      if (patch.mbti !== undefined) {
        if (patch.mbti === null) delete f.userEdited.mbti
        else f.userEdited.mbti = patch.mbti
      }
      storage.saveFriends(JSON.parse(JSON.stringify(friends.value)))
      fireSaved()
    }
    async function clear() {
      friends.value = []; report.value = null; storage.clearAll(); rawStore.clear()
    }
    return { friends, report, hasData, hydrate, setData, updateFriend, clear, setOnSaved }
  })
```

> `clear()` 不触发备份（清空是用户主动、且不应把空数据推上云覆盖备份）。

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/stores/__tests__/data.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/stores/data.ts packages/miniapp/src/stores/__tests__/data.test.ts
git commit -m "feat(miniapp): data store 保存后经 onSaved 触发防抖备份"
```

---

## Task 7: 样本放大（每人 60 条 / 每条 120 字）

**Files:**
- Modify: `packages/miniapp/src/adapters/parseLocal.ts`
- Test: `packages/miniapp/src/adapters/__tests__/parseLocal.test.ts`

**Interfaces:**
- Consumes: core `extractFriendSamples(conversations, { maxPerFriend, maxChars })`（既有）。
- Produces: `parseLocal` 产出的 `samples`/`recentSamples` 每人上限 60、单条 ≤120 字。

- [ ] **Step 1: 写失败测试**

追加到 `packages/miniapp/src/adapters/__tests__/parseLocal.test.ts`。构造一个含 80 条纯文本消息的 txt（超过 60），断言样本上限：

```ts
it('样本每人上限 60 条，单条不超过 120 字', () => {
  const lines: string[] = []
  for (let i = 0; i < 80; i++) {
    lines.push(`2025-03-01 10:${String(i % 60).padStart(2, '0')}:00 张三`)
    lines.push('内'.repeat(200)) // 200 字，超过 120
    lines.push('')
  }
  const out = parseLocal([{ name: '张三.txt', content: lines.join('\n') }], 2025)
  const s = Object.values(out.samples)[0]
  expect(s.length).toBeLessThanOrEqual(60)
  expect(s.length).toBeGreaterThan(30) // 证明确实放大了（默认 30 会正好卡 30）
  for (const line of s) expect(line.length).toBeLessThanOrEqual(120 + 3) // 「对方：」前缀约 3 字
})
```

> 若真实 txt 头行格式与假设不符导致解析不出会话，改用 parseLocal.test 中既有的构造样例风格（复用其顶部 helper/常量）。核心断言不变：上限 60、单条截断到 120。

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/parseLocal.test.ts`
Expected: FAIL（默认 30 条 → `length` 不大于 30，`toBeGreaterThan(30)` 失败）

- [ ] **Step 3: 实现**

改 `parseLocal.ts`：顶部加常量并在两处 `extractFriendSamples` 调用传入。

```ts
/** miniapp 侧样本留存参数：比 core 默认(30/80)更大，供 AI 分析与问答 agent。 */
const SAMPLE_OPTS = { maxPerFriend: 60, maxChars: 120 }
```

- `computeRecentInsights` 内：`extractFriendSamples(recentConvs, SAMPLE_OPTS)`
- `parseLocal` 内：`const samples = extractFriendSamples(conversations, SAMPLE_OPTS)`

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @nianlun/miniapp exec vitest run src/adapters/__tests__/parseLocal.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/miniapp/src/adapters/parseLocal.ts packages/miniapp/src/adapters/__tests__/parseLocal.test.ts
git commit -m "feat(miniapp): 样本留存放大至每人60条/120字(喂AI与问答agent)"
```

---

## Task 8: 真机接线 —— 云函数 + cloudBackup 单例 + 启动自动恢复 + UI

> 本任务是「碰真机 wx.cloud」的薄胶水层，沿用 `wxIO/wxBackend/wxRawFs` 的既有约定：无单测、真机验证。所有 `wx.*` 在函数体内惰性访问。

**Files:**
- Create: `packages/miniapp/cloudfunctions/getOpenId/index.js`, `.../package.json`
- Modify: `packages/miniapp/src/adapters/cloudBackup.ts`（追加真机单例导出）
- Modify: `packages/miniapp/src/adapters/storage.ts`（若 Task 5 未加 lastBackupAt，则在此加）
- Modify: `packages/miniapp/src/App.vue`
- Modify: `packages/miniapp/src/pages/overview/overview.vue`

**Interfaces:**
- Consumes: `makeCloudBackup`、`makeCloudBackup` 的 `CloudBackupDeps`、`storage`、`useDataStore`、`createBackupStore`。
- Produces: 运行时单例 `cloudBackup`、`useBackupStore`；App 启动自动恢复；概览页两个按钮。

- [ ] **Step 1: 云函数 getOpenId**

`packages/miniapp/cloudfunctions/getOpenId/package.json`：

```json
{ "name": "getOpenId", "version": "1.0.0", "main": "index.js", "dependencies": { "wx-server-sdk": "~2.6.3" } }
```

`packages/miniapp/cloudfunctions/getOpenId/index.js`：

```js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
exports.main = async () => ({ openid: cloud.getWXContext().OPENID })
```

- [ ] **Step 2: cloudBackup 真机单例（wx.cloud 接线）**

在 `cloudBackup.ts` 末尾追加真机 deps 与单例。**下载按路径需要 fileID**，微信云存储无「按路径下载」API，故用**云数据库存指针**：备份后把 `fileID` 写入集合 `nianlun_backup`（权限「仅创建者可读写」，自动按 `_openid` 隔离），恢复时查库取 `fileID` 再 `downloadFile`。

```ts
// —— 真机 wx.cloud 接线（薄封装，真机验证；wx 惰性访问）——
// 需先在云开发控制台创建集合 nianlun_backup（权限：仅创建者可读写），并部署 getOpenId。
async function wxOpenId(): Promise<string> {
  const res = await wx.cloud.callFunction({ name: 'getOpenId' })
  return (res.result as { openid: string }).openid
}
// 逻辑路径 → 云存储 cloudPath（按 openid 隔离）
async function cloudPathFor(logical: string): Promise<string> {
  const openid = await wxOpenId()
  return `nianlun-backup/${openid}/${logical}`
}
function tempFile(name: string): string { return `${wx.env.USER_DATA_PATH}/__bk_${name.replace(/\//g, '_')}` }

const wxDeps: CloudBackupDeps = {
  storage: { exportAll: () => storage.exportAll(), importAll: (s) => storage.importAll(s) },
  gzip: (d) => gzipSync(d, { level: 4 }),
  gunzip: (d) => gunzipSync(d),
  now: () => Date.now(),
  upload: async (logical, bytes) => {
    const fm = wx.getFileSystemManager()
    const tmp = tempFile(logical)
    fm.writeFileSync(tmp, bytes.buffer as ArrayBuffer)          // 字节 → 临时文件
    const cloudPath = await cloudPathFor(logical)
    const up = await wx.cloud.uploadFile({ cloudPath, filePath: tmp })
    fm.unlinkSync(tmp)
    // 记录 fileID 指针到数据库（供恢复按逻辑路径找回）
    const db = wx.cloud.database()
    await db.collection('nianlun_backup').doc('self').set({ data: { [dbField(logical)]: up.fileID, updatedAt: Date.now() } })
      .catch(() => db.collection('nianlun_backup').add({ data: { _id: 'self', [dbField(logical)]: up.fileID } }))
  },
  download: async (logical) => {
    const db = wx.cloud.database()
    let rec: any
    try { rec = (await db.collection('nianlun_backup').doc('self').get()).data } catch { return null }
    const fileID = rec?.[dbField(logical)]
    if (!fileID) return null
    try {
      const res = await wx.cloud.downloadFile({ fileID })
      const fm = wx.getFileSystemManager()
      const buf = fm.readFileSync(res.tempFilePath) as ArrayBuffer
      return new Uint8Array(buf)
    } catch { return null }
  },
}
/** 逻辑路径 → 数据库字段名（点/斜杠不合法，做映射）。 */
function dbField(logical: string): string { return 'f_' + logical.replace(/[^a-zA-Z0-9]/g, '_') }

export const cloudBackup = makeCloudBackup(wxDeps)
```

需在文件顶部补 `import { gzipSync, gunzipSync } from 'fflate'` 与 `import { storage } from './storage'`。

> ⚠️ 真机核对项（部署时逐条验证，微信基础库版本差异）：`wx.cloud.uploadFile` 的 `cloudPath` 覆盖语义、`downloadFile({ fileID })` 返回 `tempFilePath`、`writeFileSync` 接受 `ArrayBuffer`、数据库 `doc('self')` 固定 id 的 set/add 幂等。若 `doc('self').set` 在无记录时报错，用上面的 `.catch(add)` 兜底；确认后可简化。

- [ ] **Step 3: App.vue 启动自动恢复**

`onLaunch` 内，`await useDataStore().hydrate()` 之后追加：

```ts
  const data = useDataStore()
  data.setOnSaved(() => useBackupStore().scheduleBackup())
  if (data.friends.length === 0) {
    try {
      const ok = await useBackupStore().restoreNow()
      if (ok) await data.hydrate()
    } catch { /* 无网/云端无备份：静默，不打断启动 */ }
  }
```

顶部 import：`import { useBackupStore } from './stores/backup'`（见下 Step 4 导出）。

- [ ] **Step 4: backup store 运行时单例**

在 `stores/backup.ts` 末尾追加运行时单例（复用 Task 5 的工厂 + Task 8 的 `cloudBackup`）：

```ts
import { cloudBackup } from '../adapters/cloudBackup'
import { storage } from '../adapters/storage'
export const useBackupStore = createBackupStore({ cloudBackup, storage })
```

- [ ] **Step 5: 概览页按钮 + 上次备份时间**

在 `pages/overview/overview.vue` 底部加「数据与备份」区块：两个按钮 + 文案。脚本区：

```ts
import { useBackupStore } from '../../stores/backup'
import { useDataStore } from '../../stores/data'
const backup = useBackupStore()
const data = useDataStore()
async function onBackup() {
  await backup.backupNow()
  uni.showToast({ title: backup.status === 'error' ? '备份失败' : '已备份', icon: backup.status === 'error' ? 'none' : 'success' })
}
function onRestore() {
  uni.showModal({
    title: '从云端恢复', content: '将用云端备份覆盖本机数据，确定吗？',
    success: async (r) => {
      if (!r.confirm) return
      const ok = await backup.restoreNow()
      if (ok) { await data.hydrate(); uni.showToast({ title: '已恢复', icon: 'success' }) }
      else uni.showToast({ title: '云端暂无备份', icon: 'none' })
    },
  })
}
```

模板（放在概览页合适位置，套用既有 `.card`/`.btn-*` 样式）：

```html
<view class="card" style="margin-top:24rpx;padding:28rpx">
  <view class="eyebrow">数据与备份</view>
  <view class="muted" style="margin:12rpx 0">
    {{ backup.lastBackupAt ? '上次备份：' + new Date(backup.lastBackupAt).toLocaleString() : '尚未备份' }}
  </view>
  <view style="display:flex;gap:16rpx">
    <button class="btn-primary" style="flex:1" :loading="backup.status==='backing'" @click="onBackup">立即备份到云</button>
    <button class="btn-ghost" style="flex:1" :loading="backup.status==='restoring'" @click="onRestore">从云端恢复</button>
  </view>
</view>
```

- [ ] **Step 6: 类型检查 + 全量单测**

Run: `pnpm --filter @nianlun/miniapp test`
Expected: PASS（既有 + 新增测试全绿；本任务的胶水层无新单测，但不得破坏既有测试）

- [ ] **Step 7: 提交**

```bash
git add packages/miniapp/cloudfunctions/getOpenId packages/miniapp/src/adapters/cloudBackup.ts packages/miniapp/src/stores/backup.ts packages/miniapp/src/App.vue packages/miniapp/src/pages/overview/overview.vue
git commit -m "feat(miniapp): 云备份真机接线(getOpenId云函数+启动自动恢复+概览页手动按钮)"
```

---

## Task 9: 真机联调验证（人工，非自动化）

**Files:** 无（部署 + 真机操作）

- [ ] **Step 1: 部署**
  - 微信开发者工具：右键 `cloudfunctions/getOpenId` → 上传并部署（云端安装依赖）。
  - 云开发控制台：数据库新建集合 `nianlun_backup`，权限设「仅创建者可读写」；云存储权限确认「仅创建者可读写」。

- [ ] **Step 2: 基本往返**
  - 真机导入一批数据 → 概览页点「立即备份到云」→ 提示已备份。
  - 微信设置里清缓存 / 或删除小程序重进（模拟本地被清）→ 打开小程序应**自动恢复**，好友列表、词云、荐股、MBTI、AI 结果都回来。
  - 手动点「从云端恢复」→ 弹确认 → 数据覆盖回来。

- [ ] **Step 3: 上万好友压测（若有大数据源）**
  - 导入接近上万好友的数据 → 观察备份耗时、是否触发分块（看云存储是否出现 `parts/*`）、是否卡顿/闪退。
  - 据结果校准 `BIG_THRESHOLD`、`gzip level`；若单包总是过大，可把默认阈值调低使其更早分块。

- [ ] **Step 4: 记录结论**
  - 把真机核对项（Task 8 Step 2 的 ⚠️ 列表）逐条确认为「符合」或记下需调整处，更新代码注释。

---

## Self-Review（计划对照 spec）

- **spec 目标「本地空自动恢复」** → Task 8 Step 3 ✅
- **spec「备份全部处理后数据 / 整命名空间快照」** → Task 2 `exportAll` + `BACKUP_FILE_DATASETS` ✅
- **spec「gzip 压缩」** → Task 3/4/8（level 4）✅
- **spec「按 openid 隔离」** → Task 8 `cloudPathFor` + 数据库 `_openid` ✅
- **spec「防抖自动备份 + 手动按钮」** → Task 5/6/8 ✅
- **spec「性能：免双重序列化」** → Task 1 `readRaw/writeRaw` + Task 2 使用 ✅
- **spec「性能：分块降级 + 阈值」** → Task 4 ✅
- **spec「样本放大 60/120」** → Task 7 ✅
- **spec「restore 云端无备份返回 false / 版本校验」** → Task 3/4 测试覆盖 ✅
- **spec「clear 不触发备份」** → Task 6 说明 ✅
- **类型一致性**：`StorageSnapshot`（Task 2）↔ cloudBackup deps（Task 3/4）↔ `exportAll/importAll`（Task 8 wxDeps）一致；`makeCloudBackup(deps, opts?)` 签名 Task 3→4 演进兼容（opts 可选）。
- **已知留待真机的风险**：wx.cloud 具体 API 行为（Task 8 ⚠️ + Task 9）——按既有薄封装约定，真机验证而非单测。
